-- Intentionally left as a no-op.
--
-- Previous revisions attempted to manage policies on storage.objects from migrations,
-- which fails in Supabase Deploy with:
--   "must be owner of table objects"
--
-- We keep the invoices bucket (copo23-invoices) private via storage.buckets config
-- and only expose file access through signed URLs generated by Edge Functions
-- (accounting_api).
